# Password for the /admin results dashboard. Choose your own value.
ADMIN_PASSWORD=

# Free Redis database at https://console.upstash.com (Redis > Create Database),
# used to store every submitted exam result. Copy the REST URL and token from
# the database's "REST API" tab.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
